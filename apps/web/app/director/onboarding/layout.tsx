import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Director onboarding | CresOS",
  description: "Role onboarding for Directors — expectations, escalations, and Cres Dynamics how-we-run."
};

export default function DirectorOnboardingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
